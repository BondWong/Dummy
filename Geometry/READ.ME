1. To run the server, navigate to the folder that the state-server file is located in and then type ./state-server

2. states.json should be located at the same folder as state-server does

3. python3 should be installed to run the server

4. There are two images to demonstrate the running of the server
